name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: |
          # Add build commands (e.g., npm run build, mvn package)
          echo "Building..."
